1. Create virtual environment for development
    python -m venv <directory>
2. Activate the virtual environment:
    Linux: source <venv directory>/bin/Activate
    Windows: <venv directory>\Scripts\activate.bat
    Note: To deactivate just enter "deactivate"
3. Install all python requirements for project
    pip install -r requirements.txt